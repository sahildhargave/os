#include <unistd.h> 
#include <stdio.h> 
#include <string.h> 
int main()
 { int p[2]; 
char msg[15], msg1[15]; 
if (pipe(p) < 0)
 printf("error"); 
else { 
char *msg = "Hello World\n”;
 write(p[1], msg, strlen(msg) + 1); 
read(p[0], msg1, 15);
 printf("%s", msg1); 
} 
return 0; }